# План действий: модули анализа AI (оставить / переписать / удалить)

Документ фиксирует решения по текущему стеку (`emeeting-backend`, `emeeting-ui`, `ai-gateway`, `speech-service`, каталог `AI/`). Его можно обновлять по мере внедрения.

---

## 1. Оставляем как есть (без обязательной переработки)

| Область | Почему |
|---------|--------|
| **Контракты WS v1** (`docs/ANALYSIS_WS_CONTRACTS.md`) и **backward compat `emotion`** | Уже согласованы с UI и backend; ломать только осознанно с версией контракта. |
| **Backend: валидация, сохранение `analysis_event` / `analysis_report`, REST read** | Инфраструктура для отчётов и истории готова; расширять полями, а не выкидывать. |
| **Модульность конфига `ai-gateway`** (`modules.default.json`, `gateway_config.py`, реестрь плагинов) | Включение/выключение модулей без правки кода — правильная ось для диплома и эксплуатации. |
| **`face` через DeepFace + dual emit `emotion` + `face_analysis`** | Рабочий production-путь для MVP; тонкая настройка — через параметры (`min_interval_sec`, `min_confidence`), не через перепись ядра. |
| **Адаптер `speech_service.py`** (ретраи, backoff, маппинг в `text_analysis`) | Обвязка правильная; меняется только URL и поведение внешнего сервиса. |
| **UI: правый рейл транскрипта/вердикта + обработка новых типов WS** | Уже привязано к контракту; доработки — контент и UX, не архитектурный откат. |
| **`feature_store` + `report_loop` как каркас оркестрации** | Логика «снимок фич → partial report» остаётся; меняется содержимое отчёта и источники фич. |
| **Документы** `ANALYSIS_OBSERVABILITY.md`, `UI_AI_ANALYSIS_PLAN.md`, `AI/README.md`, `AI/solutions/SELECTION.md` | Референсы для команды и текста диплома — поддерживать актуальными, не удалять без замены. |

---

## 2. Нужно доработать или переписать по смыслу (не выбрасывать модуль)

| Что | Текущее состояние | Направление работ |
|-----|-------------------|-------------------|
| **`speech-service/`** | Реализованы режимы **stub** и **whisper** (faster-whisper + ffmpeg); см. `speech-service/Dockerfile` | По желанию — заменить движок на `whisperX-FastAPI` из `AI/solutions/` или коммерческий API, сохранив контракт `/v1/transcribe`. |
| **`plugins/audio.py` — блок `audio_analysis`** | Уже считаются proxy-фичи по чанку (`chunk_size_bytes`, `bitrate_kbps_est`, `speech_activity_proxy`), UI шлёт `type: "audio"` | Следующий шаг: заменить proxy на реальный DSP/SER (идеи из `emotion-recognition-using-speech` в `AI/solutions/`) и добавить калибровку/нормализацию по участнику. |
| **`report_loop` / `own_nn_client`** | Stub-агрегат + опциональный HTTP к «своей НС» | **Оставить интерфейс**, реализовать содержательный отчёт (ваш сервис по `own_nn_url` или более умный локальный сборщик без NN на первом шаге). |
| **Дублирование документации** (`ai-gateway/MEMO.md`, `CONTRACTS.md` vs `docs/*`) | Частичное пересечение | **Не переписывать логику**, а **слить или явно развести роли**: один источник истины для контракта (`docs/`), краткая памятка в репо gateway. |
| **Структура плагинов внутри `ai-gateway`** (если по backlog вынос в `modules/`) | Плагины лежат в `plugins/` | По желанию — **рефакторинг раскладки файлов** без смены протокола (косметика для масштабирования). |

---

## 3. Что можно удалить или убрать из репозитория (осознанно)

| Объект | Риск | Рекомендация |
|--------|------|----------------|
| **Отдельные клоны в `AI/solutions/*`, которые точно не будут использоваться** | Потеря локального референса без интернета | **Не удалять сразу всё.** Удалять по одному репо после того, как выписали выводы в диплом / в `SELECTION.md`, или перенести ссылку на upstream GitHub. Для экономии места — архив `.tar` или submodule позже. |
| **Дублирующие ветки экспериментов / старые скрипты** вне основного compose | Путаница | Искать через `git`; удалять только если не используются в CI и не упомянуты в доках. |
| **Legacy-only код в backend/UI** (если появится после полного перехода на `face_analysis`) | Поломка старых клиентов | **`emotion` пока не удалять** — явно помечен как legacy alias в контракте. Удаление — отдельное решение «major». |

Итого по удалению: **критичный код продукта не трогаем**; **тяжёлые reference-репозитории в `AI/solutions/`** — кандидаты на удаление только после фиксации «не используем» в этом документе или в `SELECTION.md`.

---

## 4. Рекомендуемый порядок следующих шагов

1. **Зафиксировать в конфиге URL реального ASR** и заменить stub `speech-service` → проверка `text_analysis` end-to-end (gateway → backend → UI).
2. **Аудио-фаза 2**: заменить proxy-фичи на real DSP/SER, оставить текущий клиентский поток чанков как fallback.
3. **Уточнить формат отчёта** под диплом (поля `report.summary`, per-participant) и подключить HTTP к модели или улучшить stub.
4. **Уборка `AI/solutions`**: список репо «оставить 1–2 reference» в README + удаление остальных при необходимости диска.
5. **Консолидация документации** (п. 2 таблицы выше) одним проходом.

---

## 5. Связанные файлы

- Контракты: `docs/ANALYSIS_WS_CONTRACTS.md`
- Конфиг модулей: `ai-gateway/modules.default.json`
- Отбор решений: `AI/solutions/SELECTION.md`, `AI/solutions/IMPLEMENTATION_PLAN.md`
- План UI: `docs/UI_AI_ANALYSIS_PLAN.md`
- Бэклог задач: `cursor backlog.md` (BL-030…)

---

*Версия плана: 2026-05. Обновляйте таблицы при изменении архитектуры.*
